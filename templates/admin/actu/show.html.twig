{% extends 'admin_base.html.twig' %}

{% block title %}Actu{% endblock %}

{% block body %}
    <h1>Actu</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ actu.id }}</td>
            </tr>
            <tr>
                <th>Lien_annonce</th>
                <td>{{ actu.lienAnnonce }}</td>
            </tr>
            <tr>
                <th>Contenu</th>
                <td>{{ actu.contenu }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('actu_index') }}">back to list</a>

    <a href="{{ path('actu_edit', {'id': actu.id}) }}">edit</a>

    {{ include('admin/actu/_delete_form.html.twig') }}
{% endblock %}
